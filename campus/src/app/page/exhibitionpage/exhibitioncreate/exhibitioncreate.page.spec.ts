import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ExhibitioncreatePage } from './exhibitioncreate.page';

describe('ExhibitioncreatePage', () => {
  let component: ExhibitioncreatePage;
  let fixture: ComponentFixture<ExhibitioncreatePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ExhibitioncreatePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
